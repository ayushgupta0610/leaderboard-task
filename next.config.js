/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // remotePatterns: [
    //   {
    //     protocol: "https",
    //     hostname: "bit.ly",
    //   },
    // ],
    domains: ["api.dicebear.com"],
  },
};

module.exports = nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // remotePatterns: [
    //   {
    //     protocol: "https",
    //     hostname: "bit.ly",
    //   },
    // ],
    domains: ["your-image-domain.com"],
  },
};

module.exports = nextConfig;
